.page-header
  .pull-right
    = link_to edit_admin_user_extension_path(@user_extension), class: "btn btn-warning" do
      = icon "edit", t("views.actions.edit")
    ' &nbsp;
    = link_to admin_user_extension_path(@user_extension), class: "btn btn-danger", method: :delete, data: { confirm: t('views.confirm'), disable_with: t('views.disable_with') } do
      = icon "trash", t("views.actions.destroy")
  h1= page_title(model: UserExtension.model_name.human)

dl.dl-horizontal
  - @user_extension.attributes.each do |name, value|
    dt.attribute-label
      = t("labels.user_extension.#{name}", default: name.titleize)
    dd.attribute-data
      = value
