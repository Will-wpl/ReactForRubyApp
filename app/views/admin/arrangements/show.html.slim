.page-header
  .pull-right
    = link_to edit_admin_arrangement_path(@arrangement), class: "btn btn-warning" do
      = icon "edit", t("views.actions.edit")
    ' &nbsp;
    = link_to admin_arrangement_path(@arrangement), class: "btn btn-danger", method: :delete, data: { confirm: t('views.confirm'), disable_with: t('views.disable_with') } do
      = icon "trash", t("views.actions.destroy")
  h1= page_title(model: Arrangement.model_name.human)

dl.dl-horizontal
  - @arrangement.attributes.each do |name, value|
    dt.attribute-label
      = t("labels.arrangement.#{name}", default: name.titleize)
    dd.attribute-data
      = value
